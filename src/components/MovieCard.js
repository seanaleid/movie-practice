// general imports 
import React from "react"

const MovieCard = props => {
    console.log(props)

    return (
        <div>
            <div>

            </div>
        </div>
    )
}

export default MovieCard